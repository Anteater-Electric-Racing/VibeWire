/**
 * Seed data is loaded at runtime from /harnesses/fsae-car.json via fetch().
 * This module exists as a placeholder for any future inline seed data needs.
 *
 * The canonical data files:
 *   - harnesses/fsae-car.json — harness data (enclosures, PCBs, connectors, wires, signals)
 *   - connector-library.json  — connector type definitions
 *   - layouts.json            — persisted node positions (auto-generated)
 */
export {};
