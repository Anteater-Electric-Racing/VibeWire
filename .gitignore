# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
__pycache__/
*.py[cod]

# Editor and AI assistant directories
.vscode/*
!.vscode/extensions.json
.idea
# Cursor local state; tracked project rules are the exception.
.cursor/*
!.cursor/rules/
.cursor/rules/*
!.cursor/rules/*.mdc
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scratch project roots created by the test scripts
.tmp-*

# Per-machine local identity (do not sync)
.vibewire-user

# Collaboration state is tracked (activity, users, checkpoints, etc.),
# except the session signing secret and bulky byte-history snapshots.
vibewire-state/secret.txt
vibewire-state/history/
vibewire-state/rollback-staging/
